import { createReducer } from "@reduxjs/toolkit";

const ingredientsReducer = createReducer([], builder => {
  builder
    .addCase()
    .addCase()
    .addCase()
})


export default ingredientsReducer