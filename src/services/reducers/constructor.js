import { createReducer } from "@reduxjs/toolkit";

const constructorReducer = createReducer([], builder => {
  builder
    .addCase()
    .addCase()
    .addCase()
})

export default constructorReducer;