import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  createdOder: {},
}

export const orderDetailsSlice = createSlice({
  name: 'orderDetails',
  initialState,
  reducers: {
    
  }
})